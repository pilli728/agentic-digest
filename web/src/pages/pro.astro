---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const premium = await getCollection('premium');
const sorted = premium.sort((a, b) =>
  new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);

const proContent = sorted.filter(p => p.data.tier === 'pro' || p.data.tier === 'founding');
---

<BaseLayout title="Pro Content" description="Premium playbooks, guides, and resources for AI builders">
  <header class="pro-header">
    <a href="/" class="back-link">← Back to Agentic Edge</a>
    <h1>Pro Library</h1>
    <p>Playbooks, guides, and resources for serious AI builders.</p>
  </header>

  <div class="content-grid">
    {proContent.map(item => (
      <a href={`/pro/${item.slug}`} class="content-card">
        <div class="card-meta">
          <span class={`tier-badge ${item.data.tier}`}>{item.data.tier === 'founding' ? 'Founding Only' : 'Pro'}</span>
          <span class="category">{item.data.category}</span>
        </div>
        <h3>{item.data.title}</h3>
        <p>{item.data.description}</p>
      </a>
    ))}
  </div>

  <style>
    .pro-header {
      text-align: center;
      padding: 2rem 0;
      margin-bottom: 2rem;
      border-bottom: 1px solid var(--color-border);
    }
    .back-link { display: inline-block; margin-bottom: 1rem; font-size: 0.9em; color: #888; }
    .pro-header h1 { font-size: 2.5em; margin: 0 0 0.5rem; }
    .pro-header p { color: #666; }

    .content-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 1.5rem;
    }

    .content-card {
      border: 1px solid var(--color-border);
      border-radius: 12px;
      padding: 1.5rem;
      text-decoration: none;
      color: inherit;
      transition: all 0.2s;
    }
    .content-card:hover {
      border-color: var(--color-accent);
      box-shadow: 0 4px 12px rgba(0,102,204,0.1);
    }

    .card-meta {
      display: flex;
      gap: 0.5rem;
      margin-bottom: 1rem;
    }

    .tier-badge {
      padding: 0.25rem 0.75rem;
      border-radius: 12px;
      font-size: 0.75em;
      font-weight: 600;
    }
    .tier-badge.pro { background: rgba(0,102,204,0.1); color: var(--color-accent); }
    .tier-badge.founding { background: rgba(245,158,11,0.1); color: #b45309; }

    .category {
      padding: 0.25rem 0.75rem;
      border-radius: 12px;
      font-size: 0.75em;
      background: rgba(0,0,0,0.05);
      color: #666;
    }

    .content-card h3 { margin: 0 0 0.5rem; font-size: 1.1em; line-height: 1.3; }
    .content-card p { margin: 0; font-size: 0.9em; color: #666; line-height: 1.4; }
  </style>
</BaseLayout>
